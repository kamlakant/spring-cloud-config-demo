---
spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/kamlakant/spring-cloud-config
          searchPaths: demo-app
          deleteUntrackedBranches: true
          forcePull: true
          defaultLabel: demo
server:
  port: 8888